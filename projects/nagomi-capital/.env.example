# Nagomi Capital Configuration
# 
# SECURITY NOTICE:
# This file contains ONLY non-sensitive configuration.
# ALL secrets (API keys, tokens) are stored in HashiCorp Vault at:
#   ~/.openclaw/credentials/
#
# Required Vault credentials:
#   - alpaca-api-key        (Alpaca API Key)
#   - alpaca-secret-key     (Alpaca API Secret)
#   - openrouter-api-key    (OpenRouter API Key for Grok access)
#   - x-api-bearer-token    (X/Twitter App Key)
#   - x-api-secret          (X/Twitter App Secret)
#   - x-access-token        (X/Twitter Access Token)
#   - x-access-secret       (X/Twitter Access Secret)
#   - telegram-bot-token    (Telegram Bot Token)
#   - telegram-chat-id      (Telegram Chat ID for alerts)
#
# Credential files must have permissions 600 (rw-------)
# The application will fail to start if credentials are missing or have
# incorrect permissions.
#
# For credential setup, see docs/SECURITY.md

# ============================================================================
# NON-SENSITIVE CONFIGURATION ONLY
# ============================================================================

# Alpaca Configuration
ALPACA_ENDPOINT=https://paper-api.alpaca.markets

# Trading Configuration
PAPER_TRADING=true
BASE_POSITION_SIZE_USD=1000
MAX_POSITION_SIZE_USD=5000
MAX_DAILY_LOSS_USD=500

# Agent Configuration
DEBATE_DURATION_MS=180000
MIN_CONFIDENCE_SCORE=5.0
MAX_RISK_SCORE=8.0

# Database
DATABASE_PATH=./data/nagomi.db

# Environment
NODE_ENV=development
LOG_LEVEL=info
